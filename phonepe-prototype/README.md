# PhonePe Figma Prototype

React + Vite implementation of the supplied PhonePe-style Figma mobile prototype.

## Run locally
npm install
npm run dev

## Build
npm run build

## Deploy
Import this GitHub repository into Vercel. Set the project root to phonepe-prototype, framework to Vite, build command to npm run build, and output directory to dist.
